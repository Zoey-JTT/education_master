export const mutations = {
  // 修改用户信息
  setToken(state, params) { // 修改用户Token
    state.token = params
  },
  // setUserInfo(state, params) {   // 修改个人信息
  //   state.userInfo = params
  // },
  // setRole(state, params) {   // 修改申报ID
  //   state.role = params
  // }
}
